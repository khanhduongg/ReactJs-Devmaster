import * as types from "../constants";
export const actChangeUserName = (value) => {
  return {
    type: types.CHANGE_USERNAME,
    data: value,
  };
};
export const actChangeCouserName = (value) => {
  return {
    type: types.CHANGE_USERNAME,
    data: value,
  };
};
